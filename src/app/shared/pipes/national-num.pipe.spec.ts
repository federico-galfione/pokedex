import { NationalNumPipe } from './national-num.pipe';

describe('NationalNumPipe', () => {
  it('create an instance', () => {
    const pipe = new NationalNumPipe();
    expect(pipe).toBeTruthy();
  });
});
